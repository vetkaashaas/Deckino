<UserControl x:Class="Deckino.Tools.Views.PhotoLibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="28,24,28,28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="CAMERA DATASET" Style="{StaticResource EyebrowText}" />
                <TextBlock Text="{Binding DisplayName}" Style="{StaticResource PageTitle}" Margin="0,4,0,0" />
                <TextBlock Text="{Binding Description}" Style="{StaticResource PageDescription}" Margin="0,6,0,0" />
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                <Button Content="Open photos folder" Command="{Binding OpenPhotosFolderCommand}"
                        Style="{StaticResource CompactButton}" Margin="0,0,8,0" />
                <Button Content="Refresh library" Command="{Binding RefreshCommand}"
                        Style="{StaticResource CompactButton}" />
            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Style="{StaticResource Panel}" Padding="14" Margin="0,18,0,14">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Orientation="Horizontal">
                    <Button Content="All" Command="{Binding SetFilterCommand}" CommandParameter="All" Style="{StaticResource CompactButton}" Margin="0,0,7,0" />
                    <Button Content="Annotated" Command="{Binding SetFilterCommand}" CommandParameter="Annotated" Style="{StaticResource CompactButton}" Margin="0,0,7,0" />
                    <Button Content="Unannotated" Command="{Binding SetFilterCommand}" CommandParameter="Unannotated" Style="{StaticResource CompactButton}" />
                </StackPanel>
                <TextBlock Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="{StaticResource Text.Secondary}">
                    <Run Text="{Binding MatchingCount, Mode=OneWay}" FontWeight="SemiBold" /><Run Text=" matching  ·  " />
                    <Run Text="{Binding SelectedCount, Mode=OneWay}" FontWeight="SemiBold" /><Run Text=" selected" />
                </TextBlock>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Select all matching" Command="{Binding SelectAllMatchingCommand}" Style="{StaticResource CompactButton}" Margin="0,0,7,0" />
                    <Button Content="Clear selection" Command="{Binding ClearSelectionCommand}" Style="{StaticResource CompactButton}" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource InsetPanel}" Padding="8">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding PageItems}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel /></ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Width="226" Margin="7" Style="{StaticResource Panel}" Padding="10">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="150" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <Border Background="{StaticResource Bg.Inset}" CornerRadius="7" ClipToBounds="True">
                                        <Image Source="{Binding Thumbnail}" Stretch="Uniform" />
                                    </Border>
                                    <Grid Grid.Row="1" Margin="0,9,0,0">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding FileName}" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" ToolTip="{Binding RelativePath}" />
                                        <CheckBox Grid.Column="1" IsChecked="{Binding IsSelected, Mode=TwoWay}" Margin="8,0,0,0" VerticalAlignment="Center" />
                                    </Grid>
                                    <TextBlock Grid.Row="2" Text="{Binding Dimensions}" Foreground="{StaticResource Text.Muted}" FontSize="11" Margin="0,4,0,0" />
                                    <Grid Grid.Row="3" Margin="0,8,0,0">
                                        <Grid.ColumnDefinitions><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding CaptureCondition}" Foreground="{StaticResource Text.Muted}" FontSize="11" TextTrimming="CharacterEllipsis" />
                                        <Border Grid.Column="1" x:Name="StatusBadge" Background="{StaticResource Bg.PanelRaised}" CornerRadius="6" Padding="7,3">
                                            <TextBlock x:Name="StatusText" Text="{Binding AnnotationStatus}" FontSize="10" FontWeight="SemiBold" />
                                        </Border>
                                    </Grid>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding Photo.IsAnnotated}" Value="True">
                                    <Setter TargetName="StatusBadge" Property="Background" Value="{StaticResource Success.Subtle}" />
                                    <Setter TargetName="StatusText" Property="Foreground" Value="{StaticResource Success}" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Photo.HasInvalidAnnotation}" Value="True">
                                    <Setter TargetName="StatusBadge" Property="Background" Value="{StaticResource Warning.Subtle}" />
                                    <Setter TargetName="StatusText" Property="Foreground" Value="{StaticResource Warning}" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions><ColumnDefinition Width="Auto" /><ColumnDefinition Width="*" /><ColumnDefinition Width="Auto" /></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Content="Delete annotations" Command="{Binding DeleteAnnotationsCommand}" Style="{StaticResource DangerButton}" Margin="0,0,8,0" />
                <Button Content="Delete photos + annotations" Command="{Binding DeletePhotosCommand}" Style="{StaticResource DangerButton}" />
            </StackPanel>
            <TextBlock Grid.Column="1" Text="{Binding Status}" Foreground="{StaticResource Text.Secondary}" VerticalAlignment="Center"
                       HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="18,0" />
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button Content="Previous" Command="{Binding PreviousPageCommand}" Style="{StaticResource CompactButton}" Margin="0,0,8,0" />
                <TextBlock Text="{Binding PageLabel}" VerticalAlignment="Center" Margin="6,0,14,0" />
                <Button Content="Next" Command="{Binding NextPageCommand}" Style="{StaticResource CompactButton}" />
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
